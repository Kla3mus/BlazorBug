@page "/"

<h1>Hello, world!</h1>

<span> <a href="/counter">The Counter</a> is placed in the normal folder. works fine.</span>
<br/>
<div>
    <a href="/counter_e">The Extern Counter</a> is placed in the Extern project, and connected with
    <div class="alert alert-secondary">AdditionalAssemblies="new []{ typeof(Class1).Assembly }".</div>
    It loads fine, but the "Click Me" does not work.
</div>

